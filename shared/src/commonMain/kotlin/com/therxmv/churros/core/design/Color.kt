package com.therxmv.churros.core.design

import androidx.compose.ui.graphics.Color

// ── Brand ────────────────────────────────────────────────────────────────────
val Honey500 = Color(0xFFE9B44C)
val Honey600 = Color(0xFFD49C2A)
val Honey300 = Color(0xFFF3CD75)
val Espresso = Color(0xFF4E342E)

// ── Light backgrounds ─────────────────────────────────────────────────────────
val BackgroundLight      = Color(0xFFFFF9F1)
val SurfaceLight         = Color(0xFFFFFFFF)
val SurfaceVariantLight  = Color(0xFFFCF5EA)
val DividerLight         = Color(0xFFE7D9C5)

// ── Light text ────────────────────────────────────────────────────────────────
val TextPrimaryLight    = Color(0xFF4E342E)
val TextSecondaryLight  = Color(0xFF7A645A)
val TextTertiaryLight   = Color(0xFFA38E84)
val TextDisabledLight   = Color(0xFFC8B8A8)

// ── Dark backgrounds ──────────────────────────────────────────────────────────
val BackgroundDark      = Color(0xFF201A17)
val SurfaceDark         = Color(0xFF2A221E)
val SurfaceElevatedDark = Color(0xFF332923)
val DividerDark         = Color(0xFF463932)

// ── Dark text ─────────────────────────────────────────────────────────────────
val TextPrimaryDark    = Color(0xFFF8F4EE)
val TextSecondaryDark  = Color(0xFFD7C8BA)
val EspressoAccentDark = Color(0xFFB79A8A)

// ── Semantic (same in both themes) ────────────────────────────────────────────
val SemanticSuccess = Color(0xFF67B36A)
val SemanticInfo    = Color(0xFF5C8DF6)
val SemanticEvent   = Color(0xFFA685E2)
val SemanticWarning = Color(0xFFE9B44C)
val SemanticError   = Color(0xFFD96C4F)

// ── Sticky note backgrounds — light ──────────────────────────────────────────
val StickyShoppingLight  = Color(0xFFE7F0FF)
val StickyChoresLight    = Color(0xFFE6F5E8)
val StickyReminderLight  = Color(0xFFFFF2CC)
val StickyEventLight     = Color(0xFFF1EAFF)
val StickyImportantLight = Color(0xFFFFE4D8)

// ── Sticky note backgrounds — dark (~20% darker) ─────────────────────────────
val StickyShoppingDark  = Color(0xFF1F2E47)   // #E7F0FF darkened ~20%
val StickyChoresDark    = Color(0xFF1E3A22)   // #E6F5E8 darkened ~20%
val StickyReminderDark  = Color(0xFF403515)   // #FFF2CC darkened ~20%
val StickyEventDark     = Color(0xFF2E2347)   // #F1EAFF darkened ~20%
val StickyImportantDark = Color(0xFF472820)   // #FFE4D8 darkened ~20%
