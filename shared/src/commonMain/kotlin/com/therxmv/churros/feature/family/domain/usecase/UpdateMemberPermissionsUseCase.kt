package com.therxmv.churros.feature.family.domain.usecase

/**
 * Stub — not yet applicable.
 *
 * The `household_members` table does not currently have per-member permission columns
 * (require_chore_approval, can_self_assign_chores, can_claim_rewards). This use case
 * will be implemented once the schema is extended with those columns.
 *
 * TODO: Implement when schema adds permission columns to household_members (issue #58).
 *       Expected parameters: userId, requireChoreApproval, canSelfAssign, canClaimRewards
 */
class UpdateMemberPermissionsUseCase
